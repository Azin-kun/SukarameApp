export default function OrderPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1 style={{ fontFamily: 'var(--font-heading)', color: 'var(--color-cream)' }}>Pesan</h1>
      <p>OrderPage — cart & RPC create_web_order belum di-port (Fase 2).</p>
    </div>
  )
}
