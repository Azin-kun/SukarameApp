# Salin ke `.env` (di-gitignore) dan isi lokal. Nilai anon key aman untuk
# di-bundle ke client (bukan service_role) — lihat Sukarame/.env untuk nilai staging.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
